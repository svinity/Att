rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Global safety net: default deny
    match /{document=**} {
      allow read, write: if false;
    }

    // Helper for document IDs to prevent injection
    function isValidId(id) {
      return id is string && id.size() <= 128 && id.matches('^[a-zA-Z0-9_\\-]+$');
    }

    // 1. Workers Collection
    match /workers/{workerId} {
      allow get: if isValidId(workerId);
      allow list: if true;
      allow create, update, delete: if isValidId(workerId);
    }

    // 2. Attendance Collection
    match /attendance/{attendanceId} {
      allow get: if isValidId(attendanceId);
      allow list: if true;
      allow create, update, delete: if isValidId(attendanceId);
    }

    // 3. Transactions Collection
    match /transactions/{transactionId} {
      allow get: if isValidId(transactionId);
      allow list: if true;
      allow create, update, delete: if isValidId(transactionId);
    }

    // 4. Expenses Collection
    match /expenses/{expenseId} {
      allow get: if isValidId(expenseId);
      allow list: if true;
      allow create, update, delete: if isValidId(expenseId);
    }

    // 5. Contractors Collection
    match /contractors/{contractorId} {
      allow get: if isValidId(contractorId);
      allow list: if true;
      allow create, update, delete: if isValidId(contractorId);
    }

    // 6. Contractor Payments Collection
    match /contractor_payments/{paymentId} {
      allow get: if isValidId(paymentId);
      allow list: if true;
      allow create, update, delete: if isValidId(paymentId);
    }

    // 7. Users Collection
    match /users/{userId} {
      allow get: if isValidId(userId);
      allow list: if true;
      allow create, update, delete: if isValidId(userId);
    }
  }
}
